export function ReportingPage() {
  return (
    <div className="space-y-6">
      <div className="flex items-center justify-between">
        <div>
          <h1 className="text-2xl font-semibold text-slate-900">Reporting & Statistiques</h1>
          <p className="text-sm text-slate-600 mt-1">Tableaux de bord et indicateurs de performance</p>
        </div>
        <div className="flex items-center gap-2">
          <button className="inline-flex items-center gap-2 px-4 py-2.5 text-sm font-medium text-slate-700 bg-white border border-slate-200 hover:bg-slate-50 rounded-lg transition">
            <svg className="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
            </svg>
            Exporter PDF
          </button>
          <button className="inline-flex items-center gap-2 px-4 py-2.5 text-sm font-medium text-slate-700 bg-white border border-slate-200 hover:bg-slate-50 rounded-lg transition">
            <svg className="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
            </svg>
            Exporter Excel
          </button>
        </div>
      </div>

      {/* KPIs principaux */}
      <div className="grid grid-cols-1 md:grid-cols-4 gap-4">
        <div className="lanema-card p-5">
          <div className="flex items-start justify-between mb-3">
            <div className="w-10 h-10 rounded-lg bg-lanema-blue-50 flex items-center justify-center">
              <svg className="w-5 h-5 text-lanema-blue-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2" />
              </svg>
            </div>
            <span className="text-xs font-medium text-emerald-600">+18%</span>
          </div>
          <div className="text-2xl font-semibold text-slate-900 mb-1">542</div>
          <div className="text-xs text-slate-500">Essais ce mois</div>
        </div>

        <div className="lanema-card p-5">
          <div className="flex items-start justify-between mb-3">
            <div className="w-10 h-10 rounded-lg bg-emerald-50 flex items-center justify-center">
              <svg className="w-5 h-5 text-emerald-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
              </svg>
            </div>
            <span className="text-xs font-medium text-emerald-600">-12%</span>
          </div>
          <div className="text-2xl font-semibold text-slate-900 mb-1">4.2 j</div>
          <div className="text-xs text-slate-500">Délai moyen</div>
        </div>

        <div className="lanema-card p-5">
          <div className="flex items-start justify-between mb-3">
            <div className="w-10 h-10 rounded-lg bg-amber-50 flex items-center justify-center">
              <svg className="w-5 h-5 text-amber-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
              </svg>
            </div>
            <span className="text-xs font-medium text-emerald-600">+24%</span>
          </div>
          <div className="text-2xl font-semibold text-slate-900 mb-1">12.4M</div>
          <div className="text-xs text-slate-500">CA ce mois (XAF)</div>
        </div>

        <div className="lanema-card p-5">
          <div className="flex items-start justify-between mb-3">
            <div className="w-10 h-10 rounded-lg bg-rose-50 flex items-center justify-center">
              <svg className="w-5 h-5 text-rose-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z" />
              </svg>
            </div>
            <span className="text-xs font-medium text-rose-600">+2</span>
          </div>
          <div className="text-2xl font-semibold text-slate-900 mb-1">1.8%</div>
          <div className="text-xs text-slate-500">Taux de NC</div>
        </div>
      </div>

      {/* Graphiques et analyses */}
      <div className="grid grid-cols-1 lg:grid-cols-2 gap-6">
        {/* Évolution mensuelle */}
        <div className="lanema-card p-6">
          <h2 className="text-base font-semibold text-slate-900 mb-4">Évolution des essais (6 derniers mois)</h2>
          <div className="h-64 flex items-end justify-between gap-2">
            {[
              { mois: 'Juin', value: 85, label: '420' },
              { mois: 'Juil', value: 78, label: '386' },
              { mois: 'Août', value: 92, label: '455' },
              { mois: 'Sept', value: 88, label: '436' },
              { mois: 'Oct', value: 95, label: '470' },
              { mois: 'Nov', value: 100, label: '542' },
            ].map((item, i) => (
              <div key={i} className="flex-1 flex flex-col items-center">
                <div className="w-full bg-lanema-blue-100 hover:bg-lanema-blue-200 rounded-t transition relative group"
                     style={{ height: `${item.value}%` }}>
                  <div className="absolute -top-8 left-1/2 transform -translate-x-1/2 bg-slate-900 text-white text-xs px-2 py-1 rounded opacity-0 group-hover:opacity-100 transition whitespace-nowrap">
                    {item.label} essais
                  </div>
                </div>
                <div className="text-xs text-slate-600 mt-2">{item.mois}</div>
              </div>
            ))}
          </div>
        </div>

        {/* Répartition par type */}
        <div className="lanema-card p-6">
          <h2 className="text-base font-semibold text-slate-900 mb-4">Répartition par catégorie</h2>
          <div className="space-y-3">
            {[
              { categorie: 'Chimie', count: 178, color: 'lanema-blue', percent: 33 },
              { categorie: 'Mécanique', count: 165, color: 'emerald', percent: 30 },
              { categorie: 'Microbiologie', count: 108, color: 'violet', percent: 20 },
              { categorie: 'Physique', count: 65, color: 'amber', percent: 12 },
              { categorie: 'Environnement', count: 26, color: 'rose', percent: 5 },
            ].map((item, i) => (
              <div key={i}>
                <div className="flex items-center justify-between text-sm mb-1">
                  <span className="text-slate-700">{item.categorie}</span>
                  <span className="font-semibold text-slate-900">{item.count} ({item.percent}%)</span>
                </div>
                <div className="h-2 bg-slate-100 rounded-full overflow-hidden">
                  <div
                    className={`h-full bg-${item.color}-500 rounded-full`}
                    style={{ width: `${item.percent}%` }}
                  />
                </div>
              </div>
            ))}
          </div>
        </div>
      </div>

      {/* Performance par technicien */}
      <div className="lanema-card p-6">
        <h2 className="text-base font-semibold text-slate-900 mb-4">Performance des techniciens</h2>
        <div className="overflow-x-auto">
          <table className="w-full">
            <thead className="bg-slate-50 border-b border-slate-200">
              <tr>
                <th className="px-4 py-3 text-left text-xs font-semibold text-slate-600 uppercase">Technicien</th>
                <th className="px-4 py-3 text-center text-xs font-semibold text-slate-600 uppercase">Essais réalisés</th>
                <th className="px-4 py-3 text-center text-xs font-semibold text-slate-600 uppercase">En cours</th>
                <th className="px-4 py-3 text-center text-xs font-semibold text-slate-600 uppercase">Taux conformité</th>
                <th className="px-4 py-3 text-center text-xs font-semibold text-slate-600 uppercase">Délai moyen</th>
                <th className="px-4 py-3 text-center text-xs font-semibold text-slate-600 uppercase">Charge</th>
              </tr>
            </thead>
            <tbody className="divide-y divide-slate-100">
              {[
                { nom: 'Jean MBIDA', realises: 89, en_cours: 5, conformite: 98.9, delai: 3.8, charge: 75 },
                { nom: 'Marie NGOUO', realises: 76, en_cours: 4, conformite: 97.4, delai: 4.2, charge: 65 },
                { nom: 'Paul KAMGA', realises: 82, en_cours: 6, conformite: 99.2, delai: 3.5, charge: 80 },
                { nom: 'Sophie TCHUENTE', realises: 71, en_cours: 3, conformite: 96.8, delai: 4.5, charge: 55 },
              ].map((tech, i) => (
                <tr key={i} className="hover:bg-slate-50 transition">
                  <td className="px-4 py-3">
                    <div className="flex items-center gap-3">
                      <div className="w-8 h-8 rounded-full bg-lanema-blue-100 flex items-center justify-center">
                        <span className="text-sm font-semibold text-lanema-blue-600">{tech.nom.charAt(0)}</span>
                      </div>
                      <span className="text-sm font-medium text-slate-900">{tech.nom}</span>
                    </div>
                  </td>
                  <td className="px-4 py-3 text-center text-sm text-slate-700">{tech.realises}</td>
                  <td className="px-4 py-3 text-center text-sm text-slate-700">{tech.en_cours}</td>
                  <td className="px-4 py-3 text-center">
                    <span className={`text-sm font-medium ${tech.conformite >= 98 ? 'text-emerald-600' : 'text-amber-600'}`}>
                      {tech.conformite}%
                    </span>
                  </td>
                  <td className="px-4 py-3 text-center text-sm text-slate-700">{tech.delai} j</td>
                  <td className="px-4 py-3">
                    <div className="flex items-center gap-2">
                      <div className="flex-1 h-2 bg-slate-100 rounded-full overflow-hidden">
                        <div
                          className={`h-full ${tech.charge >= 75 ? 'bg-amber-500' : 'bg-emerald-500'} rounded-full`}
                          style={{ width: `${tech.charge}%` }}
                        />
                      </div>
                      <span className="text-xs text-slate-600 w-10">{tech.charge}%</span>
                    </div>
                  </td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>
      </div>

      {/* Top clients */}
      <div className="grid grid-cols-1 lg:grid-cols-2 gap-6">
        <div className="lanema-card p-6">
          <h2 className="text-base font-semibold text-slate-900 mb-4">Top 5 clients (ce mois)</h2>
          <div className="space-y-3">
            {[
              { nom: 'SOCOCE', essais: 87, ca: 3200000 },
              { nom: 'Groupe CIMAO', essais: 65, ca: 2800000 },
              { nom: 'BTP Construction', essais: 54, ca: 2100000 },
              { nom: 'Société MATERIAUXCI', essais: 42, ca: 1600000 },
              { nom: 'INFRASTRUCTURES SA', essais: 38, ca: 1500000 },
            ].map((client, i) => (
              <div key={i} className="flex items-center justify-between p-3 rounded-lg bg-slate-50">
                <div className="flex items-center gap-3">
                  <div className="w-8 h-8 rounded-lg bg-lanema-blue-100 flex items-center justify-center">
                    <span className="text-sm font-semibold text-lanema-blue-600">{i + 1}</span>
                  </div>
                  <div>
                    <div className="text-sm font-medium text-slate-900">{client.nom}</div>
                    <div className="text-xs text-slate-500">{client.essais} essais</div>
                  </div>
                </div>
                <div className="text-sm font-semibold text-slate-900">
                  {new Intl.NumberFormat('fr-FR').format(client.ca)} XAF
                </div>
              </div>
            ))}
          </div>
        </div>

        <div className="lanema-card p-6">
          <h2 className="text-base font-semibold text-slate-900 mb-4">Consommation stock (ce mois)</h2>
          <div className="space-y-3">
            {[
              { article: 'Acide chlorhydrique', quantite: 45, unite: 'L', cout: 180000 },
              { article: 'Gants nitrile', quantite: 120, unite: 'boîtes', cout: 120000 },
              { article: 'Hydroxyde de sodium', quantite: 28, unite: 'kg', cout: 98000 },
              { article: 'Papier filtre', quantite: 85, unite: 'paquets', cout: 68000 },
              { article: 'Milieu de culture PCA', quantite: 5, unite: 'kg', cout: 250000 },
            ].map((item, i) => (
              <div key={i} className="flex items-center justify-between p-3 rounded-lg bg-slate-50">
                <div>
                  <div className="text-sm font-medium text-slate-900">{item.article}</div>
                  <div className="text-xs text-slate-500">{item.quantite} {item.unite}</div>
                </div>
                <div className="text-sm font-semibold text-slate-900">
                  {new Intl.NumberFormat('fr-FR').format(item.cout)} XAF
                </div>
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>
  )
}
